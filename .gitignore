.env
backups/
certs/
.vscode/
# docs/ intentionally tracked

# Wiki (cloned separately, not tracked in main repo)
.wiki/

scenes/*.xml

# Marketing site (separate git repo)
.www/

# Python files
__pycache__/

# Custom branding — user-specific logos, not tracked in git
# Drop your logo into control-server/static/ as logo.webp, logo.png, or logo.svg
control-server/static/logo.*
landing/logo.*

# Codex
.codex

.claude